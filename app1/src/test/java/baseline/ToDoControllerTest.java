package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ToDoControllerTest {

    @Test
    void makeSaveFile() {
        // make a list of strings
        // use makeSaveFile() to write that list into a save file
        // read in from the save file to another list
        // assert that the original list and the read in list are the same
    }
}